kind = "web"
previewPath = "/"
title = "VJRA"
version = "1.0.0"
id = "artifacts/vjra"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24822

[services.development]
run = "pnpm --filter @workspace/vjra run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vjra", "run", "build" ]
serve = "static"
publicDir = "artifacts/vjra/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24822"
BASE_PATH = "/"
